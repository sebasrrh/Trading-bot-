# @tradeboard/ui

The design system: `src/tokens.css` (single source of truth for every color/space/type token) and the React primitives (Card, Button, DataTable, StatTile, DeltaChip, ...).

- Spec: [docs/02-design-system.md](../../docs/02-design-system.md) — tokens there are normative
- No raw hex anywhere else in the repo. Changing series colors requires re-running the palette validator (docs/02 §1.5).
