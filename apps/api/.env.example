# Provider keys - everything still works key-less (Stooq daily + Yahoo fallback)
ALPACA_KEY_ID=
ALPACA_SECRET=
POLYGON_KEY=

# CORS origin (comma-separated, default http://localhost:5173)
CORS_ORIGIN=http://localhost:5173

# DB path (default apps/api/data/cache.db)
CACHE_DB_PATH=